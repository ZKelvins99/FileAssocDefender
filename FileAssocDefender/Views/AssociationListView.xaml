<UserControl x:Class="FileAssocDefender.Views.AssociationListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:FileAssocDefender.Views">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="#1FFFFFFF"
                CornerRadius="8"
                Padding="16"
                Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" VerticalAlignment="Center">
                    <TextBlock Text="文件关联总览"
                               FontSize="20"
                               FontWeight="SemiBold" />
                    <TextBlock Margin="0,4,0,0" Opacity="0.75" TextWrapping="Wrap">
                        <Run Text="正常 " />
                        <Run Text="{Binding HealthyCount}" FontWeight="SemiBold" />
                        <Run Text=" / " />
                        <Run Text="{Binding TotalCount}" FontWeight="SemiBold" />
                        <Run Text="  ·  上次扫描 " />
                        <Run Text="{Binding LastScanTime}" />
                    </TextBlock>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Grid.ColumnSpan="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center">
                    <Button Content="立即刷新"
                            Command="{Binding RefreshCommand}"
                            Padding="16,8"
                            Margin="0,0,8,0" />

                    <Button Content="一键全部修复"
                            Command="{Binding FixAllCommand}"
                            Padding="16,8"
                            FontWeight="SemiBold" />
                </StackPanel>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Items}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <views:AssociationCard />
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
