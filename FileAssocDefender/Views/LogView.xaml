<UserControl x:Class="FileAssocDefender.Views.LogView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="事件日志"
                       FontSize="20"
                       FontWeight="SemiBold"
                       DockPanel.Dock="Left" />
            <Button Content="清空"
                    Command="{Binding ClearCommand}"
                    HorizontalAlignment="Right"
                    DockPanel.Dock="Right"
                    Padding="12,6" />
        </DockPanel>

        <ListView Grid.Row="1"
                  ItemsSource="{Binding Entries}"
                  BorderThickness="0">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="时间" Width="160"
                                    DisplayMemberBinding="{Binding Timestamp, StringFormat=yyyy-MM-dd HH:mm:ss}" />
                    <GridViewColumn Header="类型" Width="80"
                                    DisplayMemberBinding="{Binding EventType}" />
                    <GridViewColumn Header="详情" Width="600"
                                    DisplayMemberBinding="{Binding Message}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
