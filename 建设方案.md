# FileAssocDefender 建设方案

## 1. 项目概述

**FileAssocDefender** 是一个 Windows 11 风格的文件关联守护工具，核心能力：

1. **可视化展示** Office 等文件类型当前的默认打开方式（程序名称 + 图标）
2. **识别劫持**：当默认程序不是 Microsoft Office 时，明确标注劫持来源（如 WPS、第三方软件）并展示其图标
3. **一键修复**：将指定文件类型的默认打开方式改回本机已安装的 Microsoft Office
4. **可选守护**：后台定时巡检，发现被篡改后自动或提醒修复

### 1.1 用户场景

| 场景 | 期望行为 |
|------|----------|
| 打开应用 | 首页卡片列表展示 `.docx`、`.xlsx`、`.pptx` 等当前默认程序及图标 |
| 发现 WPS 劫持 | 对应行显示红色「已劫持」徽章，当前程序显示「WPS 文字」+ WPS 图标 |
| 点击「修复为 Office」 | 弹出 UAC 提权 → 写入正确关联 → 行变为绿色「Microsoft Word」+ Word 图标 |
| 一键全部修复 | 批量修复所有被劫持项，进度条 + 结果摘要 |
| 最小化 | 驻留系统托盘，发现劫持时气泡通知 |

## 2. 开发环境

### 2.1 本机 .NET 环境（已确认）

| 组件 | 版本 |
|------|------|
| SDK | **10.0.300** |
| WindowsDesktop Runtime | 10.0.8 |
| 目标框架 | **net10.0-windows** |

项目通过 `global.json` 锁定 SDK 10.0.300，确保团队与本机构建一致：

```json
{
  "sdk": {
    "version": "10.0.300",
    "rollForward": "latestFeature"
  }
}
```

## 3. 技术选型

| 技术 | 选择 | 原因 |
|------|------|------|
| 语言 | C# (.NET 10) | 本机已装 SDK 10.0.300；WPF 桌面支持完善 |
| UI 框架 | WPF | 成熟稳定，与 Win11 风格库配合良好 |
| UI 风格 | **WPF-UI** (Fluent Design) | 原生 Win11 视觉：Mica 背景、圆角、NavigationView、Segoe UI Variable |
| 图标提取 | Shell32 + 注册表 DefaultIcon | 从关联 EXE / ProgID 读取真实程序图标 |
| Office 探测 | 注册表 + VSTO/C2R 路径 | 自动识别本机 Office 2016/2019/365 安装 |
| 关联修复 | `IApplicationAssociationRegistration` + 注册表回退 | Win10+ 官方 API 优先，失败时写 UserChoice |
| 桌面驻留 | Hardcodet.NotifyIcon.Wpf | 系统托盘 + 右键菜单 |
| 打包 | WiX Toolset v4 | 产出 `.msi` 安装包 |
| 发布方式 | self-contained 单文件 | 用户无需单独安装 .NET Runtime |

> **为何不用 MaterialDesignInXAML**：Material Design 偏 Android/Google 风格，与 Win11 系统设置「默认应用」页面视觉不一致。WPF-UI 提供 Mica/Acrylic Backdrop、Win11 控件样式，更贴合「现代、符合 Win11」的要求。

## 4. 核心原理

### 4.1 文件关联解析（读）

Windows 文件关联由三层注册表决定（优先级从高到低）：

```
用户双击 .xlsx
  ↓
① HKCU\...\Explorer\FileExts\.xlsx\UserChoice\ProgId
  → 有值 → 使用该 ProgID
  ↓ 无值
② HKCU\Software\Classes\.xlsx（默认值）
  ↓ 无值
③ HKCR\.xlsx（默认值）
  → 解析 HKCR\<ProgID>\shell\open\command → 得到 EXE 路径
```

### 4.2 程序信息展示（名称 + 图标）

从 ProgID / EXE 路径提取 UI 所需信息：

```
ProgID / ExePath
  ↓
DefaultIcon 注册表 或 EXE 内嵌图标  →  Icon（32×32 / 48×48）
FileVersionInfo.ProductName          →  友好名称（如 "WPS Office"）
PresetStore 期望 ProgID/EXE          →  判断是否被劫持
```

**劫持判定规则：**

| 条件 | 状态 |
|------|------|
| 当前 EXE 匹配本机 Office 路径 | `Healthy`（正常） |
| 当前 EXE 为已知劫持程序（WPS 等）或 ProgID 不匹配 | `Hijacked`（已劫持） |
| Office 未安装或无法解析 | `Unknown`（未知） |

### 4.3 修复为 Microsoft Office（写）

修复优先级：

```
1. IApplicationAssociationRegistration.SetAppAsDefault(progId, extension, ...)
   ↓ 失败（权限 / 策略限制）
2. 写入 UserChoice（需管理员 + Hash 计算）
   ↓ 失败
3. 写入 HKCU\Software\Classes + 清理 OpenWithProgids 中的劫持项
```

修复前自动备份当前关联，支持从设置页还原。

## 5. 系统架构

```
┌──────────────────────────────────────────────────────────┐
│                   WPF-UI 层（Win11 风格）                  │
│  关联总览（卡片+图标） │ 日志 │ 设置 │ 系统托盘            │
└────────────────────────────┬─────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────┐
│                    业务逻辑层 (Services)                     │
│  ┌─────────────┐  ┌──────────────┐  ┌─────────────────┐  │
│  │  Scanner    │  │   Fixer      │  │    Guardian     │  │
│  │  扫描+判定   │  │  修复为Office │  │  定时巡检        │  │
│  └──────┬──────┘  └──────┬───────┘  └────────┬────────┘  │
│         │                │                    │           │
│  ┌──────▼──────┐  ┌──────▼───────┐  ┌────────▼────────┐  │
│  │ IconResolver│  │ OfficeDetector│  │  PresetStore    │  │
│  │ 程序名+图标  │  │ 探测本机Office │  │ 期望 ProgID/EXE │  │
│  └─────────────┘  └──────────────┘  └─────────────────┘  │
└────────────────────────────┬─────────────────────────────┘
                             │
┌────────────────────────────▼─────────────────────────────┐
│                     数据访问层                              │
│  RegistryHelper  │  ShellIconHelper  │  AssociationApi   │
│  三层注册表读写    │  EXE/ProgID 图标   │  官方关联 API      │
└──────────────────────────────────────────────────────────┘
```

## 6. 模块设计

### 6.1 AssociationInfo（展示模型 — 前端核心数据）

```csharp
public class AssociationInfo
{
    public string Extension;           // ".xlsx"
    public string FileTypeLabel;       // "Excel 工作簿"
    public string Category;            // "Excel" / "Word" / "PowerPoint"

    // —— 当前实际关联（UI 主展示）——
    public string CurrentProgId;       // 如 "KWPS.Document.12"
    public string CurrentAppName;      // 如 "WPS 表格"（劫持时显示劫持软件名）
    public string CurrentExePath;      // 实际 EXE 完整路径
    public ImageSource CurrentIcon;    // 32×32 程序图标（绑定到 UI）

    // —— 期望 Office 关联（修复目标）——
    public string TargetProgId;        // 如 "Excel.Sheet.12"
    public string TargetAppName;       // "Microsoft Excel"
    public string TargetExePath;       // 本机 EXCEL.EXE 路径
    public ImageSource TargetIcon;     // Office 图标（修复按钮旁预览）

    public AssociationStatus Status;   // Healthy / Hijacked / Unknown
    public string StatusMessage;       // "默认打开方式为 WPS 表格，非 Microsoft Excel"
}
```

### 6.2 IconResolver（图标与程序名解析）

```csharp
public class IconResolver
{
    ImageSource GetIconFromExe(string exePath, int size = 32);
    ImageSource GetIconFromProgId(string progId, int size = 32);
    string GetFriendlyName(string exePath, string progId);
    // 优先 FileVersionInfo.ProductName → 注册表 FriendlyTypeName → ProgID 末段
}
```

实现要点：
- EXE 存在：`SHGetFileInfo` / `Icon.ExtractAssociatedIcon`
- 仅 ProgID：读 `HKCR\<ProgID>\DefaultIcon`，支持 `"%ProgramFiles%\...\excel.exe,0"` 形式
- 图标缓存：`Dictionary<string, ImageSource>` 避免列表重复提取

### 6.3 OfficeDetector（本机 Office 探测）

```csharp
public class OfficeDetector
{
    OfficeInstallation Detect();
    // 扫描 Click-to-Run: HKLM\SOFTWARE\Microsoft\Office\ClickToRun\Configuration
    // 扫描 MSI: HKLM\SOFTWARE\Microsoft\Office\<ver>\Common\InstallRoot
    // 返回 Word/Excel/PowerPoint 的 EXE 路径 + 对应 ProgID
}

public class OfficeInstallation
{
    public string WordExePath;
    public string ExcelExePath;
    public string PowerPointExePath;
    public string WordProgId;      // "Word.Document.12"
    public string ExcelProgId;     // "Excel.Sheet.12"
    public string PowerPointProgId;// "PowerPoint.Show.12"
    public bool IsInstalled;
}
```

### 6.4 RegistryHelper（注册表读写）

```csharp
public class RegistryHelper
{
    AssociationRaw GetAssociation(string extension);
    List<AssociationRaw> ScanAll(IEnumerable<string> extensions);

    bool SetUserChoice(string extension, string progId);   // 需管理员
    bool SetHkcuDefault(string extension, string progId);
    void RemoveOpenWithProgId(string extension, string progId);
    string ResolveCommand(string progId);
}
```

### 6.5 PresetStore（期望关联知识库）

内置 Office 文件类型清单（可扩展）：

| 扩展名 | 类别 | 期望 ProgID | 常见劫持 ProgID |
|--------|------|-------------|-----------------|
| `.docx` | Word | Word.Document.12 | KWPS.Document.12 |
| `.doc` | Word | Word.Document.8 | KWPS.Document.12 |
| `.xlsx` | Excel | Excel.Sheet.12 | KWPS.Spreadsheet.12 |
| `.xls` | Excel | Excel.Sheet.8 | KWPS.Spreadsheet.12 |
| `.pptx` | PowerPoint | PowerPoint.Show.12 | KWPS.Presentation.12 |
| `.ppt` | PowerPoint | PowerPoint.Show.8 | KWPS.Presentation.12 |

首次启动调用 `OfficeDetector` 校准本机 EXE 路径；用户可在设置页增删受保护扩展名。

### 6.6 AssociationScanner（扫描引擎）

```csharp
public class AssociationScanner
{
    List<AssociationInfo> Scan();
    // 对每个 Preset：
    //   1. RegistryHelper 读当前 ProgID/EXE
    //   2. IconResolver 取 CurrentIcon + CurrentAppName
    //   3. OfficeDetector 取 TargetIcon + TargetAppName
    //   4. 比对 → 填充 Status / StatusMessage
}
```

### 6.7 AssociationFixer（修复引擎）

```csharp
public class AssociationFixer
{
    FixResult Fix(AssociationInfo item);      // 单项「修复为 Office」
    FixResult FixAll(IEnumerable<AssociationInfo> items);

    void BackupCurrent(string extension);
    void KillWpsGuardProcessIfNeeded();       // 可选：终止 WPS 守护进程
}
```

修复成功后触发 UI 刷新，更新图标与名称为 Microsoft Office。

### 6.8 Guardian（守护引擎）

```csharp
public class Guardian : IDisposable
{
    int PollingIntervalMs = 10000;
    bool AutoRepair;
    event Action<AssociationInfo> OnHijackDetected;
    event Action<AssociationInfo> OnRepaired;
}
```

### 6.9 LogService（日志服务）

记录：扫描时间、劫持发现、修复前后 ProgID/程序名变化。

## 7. UI 设计（Win11 现代风格）

设计参考：**Windows 11 设置 → 应用 → 默认应用** 页面。

### 7.1 视觉规范

| 元素 | 规范 |
|------|------|
| 窗口 | 无边框 / 自定义标题栏，Mica 背景（WPF-UI `WindowBackdropType.Mica`） |
| 字体 | Segoe UI Variable（系统字体，Win11 默认） |
| 圆角 | 卡片 8px，按钮 4px |
| 配色 | 跟随系统浅色/深色主题（`ApplicationThemeManager`） |
| 间距 | 8px 网格，卡片内边距 16px |
| 导航 | 左侧 `NavigationView`：总览 / 日志 / 设置 |

### 7.2 关联总览页（核心页面）

每项文件类型一张 **AssociationCard**：

```
┌─────────────────────────────────────────────────────────────┐
│  [.xlsx 图标]  Excel 工作簿 (.xlsx)          [已劫持] 红徽章  │
│                                                              │
│  当前默认    [WPS 图标]  WPS 表格                             │
│              C:\Program Files\...\wps.exe                   │
│                                                              │
│  修复目标    [Excel 图标]  Microsoft Excel                    │
│                                                              │
│              [ 修复为 Office ]          [ 查看详情 ▾ ]        │
└─────────────────────────────────────────────────────────────┘
```

- **Healthy**：绿色左边框 + 「正常」徽章，仅显示当前 Office 图标和名称
- **Hijacked**：红色左边框 + 「已劫持」徽章，**并排对比**当前劫持程序 vs 目标 Office（均带图标）
- **Unknown**：黄色警告 + 「Office 未检测到」提示

顶部工具栏：
- 整体健康度环形进度（如 4/6 正常）
- **「一键全部修复为 Office」** 主按钮（Accent 色，仅在有 Hijacked 项时启用）
- **「立即刷新」** 按钮

### 7.3 详情抽屉 / 弹窗

点击「查看详情」展开：
- 当前 ProgID、注册表层级（UserChoice / HKCU / HKCR）
- 完整 command 行
- OpenWithProgids 中的竞争项列表
- 「复制诊断信息」按钮

### 7.4 日志页

时间线列表：时间 | 类型（劫持/修复/系统）| 详情（含程序名变化）

### 7.5 设置页

- 受保护文件类型多选
- 巡检间隔（5s / 10s / 30s / 1min）
- 开机启动、自动修复开关
- Office 安装检测结果（只读展示路径）
- 管理员权限状态提示

### 7.6 系统托盘

- 图标：正常为盾牌，有劫持时为红色警告
- 右键：打开主窗口 / 立即扫描 / 退出
- 气泡：「.xlsx 已被 WPS 表格劫持，点击修复」

### 7.7 权限与 UAC

- 应用 manifest：`requireAdministrator` 或在修复时 `Process.Start` 提权子进程
- 首次修复前说明：「需要管理员权限以修改系统默认打开方式」

## 8. 开发计划

### Phase 1：基础骨架（1-2 天）

- [ ] 初始化 .NET 10 WPF 项目（`net10.0-windows`）
- [ ] 添加 `global.json` 锁定 SDK 10.0.300
- [ ] 引入 WPF-UI（Mica、NavigationView、主题切换）
- [ ] 搭建 MVVM 基础（ViewModelBase、RelayCommand）
- [ ] 实现 `RegistryHelper` 读取三层关联
- [ ] 实现 `IconResolver`（EXE/ProgID → 图标 + 程序名）

### Phase 2：Office 探测与扫描（2-3 天）

- [ ] 实现 `OfficeDetector`（C2R + MSI 路径探测）
- [ ] 实现 `PresetStore` + 内置 Office 扩展名清单
- [ ] 实现 `AssociationScanner`（扫描 + 劫持判定 + 填充图标/名称）
- [ ] 单元测试：注册表读取、Office 路径解析

### Phase 3：修复引擎（2 天）

- [ ] 实现 `AssociationFixer`（IApplicationAssociationRegistration + UserChoice 回退）
- [ ] 修复前备份、修复后 re-scan 刷新 UI
- [ ] WPS OpenWithProgids 清理、守护进程处理（可选）
- [ ] 实现 `Guardian` 定时巡检 + `LogService`

### Phase 4：UI 开发（2-3 天）

- [ ] 主窗口：Mica + NavigationView 布局
- [ ] **AssociationCard** 组件（图标、程序名、状态徽章、修复按钮）
- [ ] 关联总览页 + 一键修复 + 刷新
- [ ] 详情抽屉、日志页、设置页
- [ ] 系统托盘 + 劫持气泡通知
- [ ] 浅色/深色主题跟随系统

### Phase 5：打磨与发布（1-2 天）

- [ ] 首次启动引导（Office 检测 + 权限说明）
- [ ] 图标缓存、扫描性能优化
- [ ] `app.manifest` 管理员权限配置
- [ ] WiX v4 打包 `.msi`
- [ ] GitHub Actions 自动 build + publish

## 9. 打包方案

### 9.1 发布命令

```powershell
dotnet publish FileAssocDefender/FileAssocDefender.csproj `
  -c Release -r win-x64 `
  --self-contained true `
  -p:PublishSingleFile=true `
  -p:PublishReadyToRun=true `
  -p:ApplicationManifest=app.manifest
```

### 9.2 WiX 打包

- 安装路径：`%ProgramFiles%\FileAssocDefender`
- 开始菜单快捷方式
- 可选：开机启动注册表项
- 无需防火墙规则（纯本地注册表操作）

### 9.3 分发

- GitHub Releases 发布 `.msi`
- 版本号：`v1.0.0` 起

## 10. 文件结构

```
FileAssocDefender/
├── global.json                          # 锁定 SDK 10.0.300
├── Directory.Build.props                # 统一 TargetFramework、Nullable 等
├── FileAssocDefender.sln
├── FileAssocDefender/
│   ├── FileAssocDefender.csproj         # net10.0-windows
│   ├── app.manifest                     # requireAdministrator
│   ├── App.xaml / App.xaml.cs
│   ├── MainWindow.xaml / MainWindow.xaml.cs
│   ├── Models/
│   │   ├── AssociationInfo.cs           # 含 CurrentIcon / TargetIcon
│   │   ├── AssociationStatus.cs
│   │   ├── OfficeInstallation.cs
│   │   └── Preset.cs
│   ├── Services/
│   │   ├── RegistryHelper.cs
│   │   ├── IconResolver.cs              # 新增：图标+程序名
│   │   ├── ShellIconHelper.cs           # 新增：Shell32 P/Invoke
│   │   ├── OfficeDetector.cs            # 新增：Office 路径探测
│   │   ├── AssociationApi.cs            # 新增：IApplicationAssociationRegistration
│   │   ├── PresetStore.cs
│   │   ├── AssociationScanner.cs
│   │   ├── AssociationFixer.cs
│   │   ├── Guardian.cs
│   │   └── LogService.cs
│   ├── ViewModels/
│   │   ├── MainViewModel.cs
│   │   ├── AssociationListViewModel.cs
│   │   ├── AssociationCardViewModel.cs  # 单项卡片 VM
│   │   ├── LogViewModel.cs
│   │   └── SettingsViewModel.cs
│   ├── Views/
│   │   ├── AssociationListView.xaml     # 关联总览（核心页）
│   │   ├── AssociationCard.xaml       # 可复用卡片控件
│   │   ├── DetailDrawer.xaml
│   │   ├── LogView.xaml
│   │   └── SettingsView.xaml
│   ├── Converters/
│   │   └── StatusToBrushConverter.cs    # 状态 → 颜色/徽章
│   └── Resources/
│       ├── presets.json
│       └── hijack-signatures.json       # 已知劫持 ProgID/EXE 特征
├── FileAssocDefender.Tests/
│   └── ...                              # Scanner / OfficeDetector 测试
├── Installer/
│   └── FileAssocDefender.wxs
├── README.md
├── .gitignore
└── AGENTS.md
```

## 11. 编码规范

- 所有中文文件使用 **UTF-8 without BOM** 编码（见 AGENTS.md）
- C# 命名：类/方法 PascalCase，私有字段 `_camelCase`
- UI 与业务分离：MVVM，View 不含注册表逻辑
- 注释使用中文
